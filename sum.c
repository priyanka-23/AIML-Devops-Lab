#include <stdio.h>

int main() {
    // Variables are initialized with fixed values instead of using scanf
    int x = 10;
    int y = 25;
    int sum = x + y;

    // Display the result
    printf("The sum of %d and %d is: %d\n", x, y, sum);
    
    return 0;
}

